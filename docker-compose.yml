version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-portfolio:latest
    container_name: omar-portfolio
    ports:
      - "3000:80"
    restart: unless-stopped